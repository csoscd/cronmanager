# =============================================================================
# Cronmanager – Database Credentials
#
# Copy this file to db.credentials and fill in your values.
# This file is sourced by deploy.sh and used by docker-compose.yml.
#
# NEVER commit db.credentials to version control.
# =============================================================================

DB_NAME=cronmanager
DB_USER=cronmanager
DB_PASSWORD=cronmanagerpassword
DB_ROOT_USER=root
DB_ROOT_PASSWORD=rootpassword
